@* @model IEnumerable<EventManagement.Models.Event>

@{
    ViewData["Title"] = "Browse Events";
}

<div class="container">
    <h1 class="page-title">Upcoming Events</h1>

    <form asp-action="Index" method="get" class="search-form">
        <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" placeholder="Search by event title..." class="form-control" />
        <button type="submit" class="btn btn-primary">Search</button>
    </form>
    <div class="event-grid">
        @foreach (var ev in Model)
        {
            <a asp-action="Details" asp-route-id="@ev.EventId" class="event-card">
                <div class="event-card-header">
                    @* <img src="https://images.unsplash.com/photo-1523580494863-6f3031224c94?auto=format&fit=crop&q=80" alt="@ev.Title" /> *@
                </div>
                <div class="event-card-body">
                    <p class="event-card-date">@ev.EventDateTime.ToString("dddd, dd MMMM yyyy")</p>
                    <h5 class="event-card-title">@ev.Title</h5>
                </div>
                <div class="event-card-footer">
                    <span>@(ev.Registrations?.Count() ?? 0) / @ev.MaxParticipants Spots</span>
                    <span>View Details &rarr;</span>
                </div>
            </a>
        }
    </div>
</div> *@

                <div class="form-card">
<h1>Create a New Event</h1>
<form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

<div class="form-group">
<label asp-for="Title">Event Title</label>
<input asp-for="Title" class="form-control" placeholder="What is the name of your event?" required />
                <span asp-validation-for="Title" class="text-danger"></span>
</div>

<div class="form-group">
<label asp-for="Description">Description</label>
<textarea asp-for="Description" class="form-control" placeholder="Tell us more about the event" rows="4"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <!-- New Venue Field -->
            <div class="form-group">
                <label asp-for="Venue">Venue</label>
                <input asp-for="Venue" class="form-control" placeholder="Where will this event take place?" required />
                <span asp-validation-for="Venue" class="text-danger"></span>
</div>

<div class="form-group">
<label asp-for="EventDateTime">Date and Time</label>
<input asp-for="EventDateTime" class="form-control" type="datetime-local" required />
                <span asp-validation-for="EventDateTime" class="text-danger"></span>
</div>

<div class="form-group">
<label asp-for="Fees">Registration Fee ($)</label>
<input asp-for="Fees" class="form-control" type="number" step="0.01" placeholder="e.g., 5.00 or 0.00 for free" required />
                <span asp-validation-for="Fees" class="text-danger"></span>
</div>

<div class="form-group">
<label asp-for="MaxParticipants">Maximum Participants</label>
<input asp-for="MaxParticipants" class="form-control" type="number" placeholder="e.g., 50" required />
                <span asp-validation-for="MaxParticipants" class="text-danger"></span>
</div>

            <button type="submit" class="btn btn-primary" style="width:100%; padding-top: 1rem; padding-bottom: 1rem;">Create Event</button>
            <button type="submit" class="btn btn-primary" style="width:100%; padding-top: 1rem; padding-bottom: 1rem;">
                Create Event
            </button>
</form>
</div>
</div>
</div>